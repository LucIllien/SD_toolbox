import numpy as np
from numba import jit

def integration_data(ydata, dt, event_indexes,y_bound,time_bound):
    
    return integrated_measure


def derivative_data(ydata, dt, event_indexes)

    return variation_measure